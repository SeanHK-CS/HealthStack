# Test tooling only (README: `npm install jsdom`); the site itself has no build step.
node_modules/
package.json
package-lock.json

# Product plans stay out of the public repo (kept in the owner's private Drive).
ROADMAP.md
